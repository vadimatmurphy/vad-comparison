[project]
name = "vad-comparison"
version = "0.1.0"
description = "Compare Silero VAD v5 vs v6 — live mic recording with side-by-side EOS events"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "livekit-agents==1.2.15",
    "livekit-plugins-silero==1.2.15",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "websockets>=14.0",
]
